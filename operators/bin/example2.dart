void main(List<String> args) {
  /// we have four types of operators in dart
  /// unaty prefix, unary postfix, binary infix, compound assignment

  /// Unary prefix
  int age = 30;
  int age2 = --age;
  print(age);
  print(age2);

  ///unary bitwise complement prefix operator
  print(~1);
}
